load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

alias(
    name = "dummy",
    actual = select({
        "@platforms//:incompatible": ["never_used"],
    }, no_match_error = "This target is not provided by this version of CUDA"),
)
