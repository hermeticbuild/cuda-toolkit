# AGENTS Guide

## Working Conventions

1. Prefer deterministic attribute-driven behavior over implicit environment behavior.
2. Remove obsolete env-driven code paths instead of preserving compatibility.
3. Keep docs/examples aligned with the final model (no fallback paths).
